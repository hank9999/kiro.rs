services:
  kiro-rs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kiro-rs
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "18990:8990"
    volumes:
      - ./config/:/app/config/
    restart: unless-stopped
